[project]
name = "muisc-director"
version = "0.1.0"
description = "Suno AI music director — DJ-ready prompt engineering"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["python"]
