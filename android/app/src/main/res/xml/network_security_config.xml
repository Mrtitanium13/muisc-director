<?xml version="1.0" encoding="utf-8"?>
<!-- Allow http:// to local PC / emulator (uvicorn). Railway still uses https. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
